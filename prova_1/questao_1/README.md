Para rodar, é necessário ter o [NodeJS](https://nodejs.org/) instalado.

Instale as dependências com o comando:
```bash
npm i
```

Rode o projeto com o comando "br" (build and run)
```bash
npm run br
```